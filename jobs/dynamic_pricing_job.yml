$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
command: >
  python src/main.py
code: .
environment:
  build:
    path: ./conda
compute: azureml:tunitfeatureeng # Replace with your cluster name
experiment_name: dynamic_pricing_ci_cd
display_name: Run Dynamic Pricing Pipeline
description: Submit main pipeline from GitHub to Azure ML

environment_variables:
  TABLE_CONNECTION_STRING: "${{secret:BLOB_CONNECTION_STRING}}"
